@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Générer des codes de récupération d’authentification à deux facteurs (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Mettez ces codes en lieu sûr.</strong>
    </p>
    <p>
        Si vous perdez votre appareil et n’avez pas les codes de récupération, vous perdrez l’accès à votre compte.
    </p>
    <p>
        Générer de nouveaux codes de récupération ne change pas les clés utilisées dans les applications d’authentification. Si vous souhaitez changer la clé
                utilisé dans une application d’authentification que vous devez <a asp-page="./ResetAuthenticator">réinitialiser vos clés d’authentification.</a>
    </p>
</div>
<div>
    <form method="post">
        <button class="btn btn-danger" type="submit">Générer des codes de récupération</button>
    </form>
</div>
